// Static HTML site (devashribuilders) is served via /app/frontend/public/index.html
// React root is intentionally empty so the static markup renders unaffected.
function App() {
  return null;
}

export default App;
